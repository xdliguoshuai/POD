根据分析，你的项目目前存在两套非常接近但不一样的主题色：

1.  **定义的主题色** (`src/styles/index.css`):
    -   `--primary`: **`165 45% 18%`** (近似 `#194236`，偏蓝绿色)
2.  **实际使用的主题色** (硬编码在多个组件中):
    -   Hex: **`#1a472a`** (近似 HSL `141 46% 19%`，典型的森林绿)
    -   广泛存在于 `HomePage.tsx`, `MainLayout.tsx`, `ApparelModels.tsx` 等核心文件中。

看起来 **`#1a472a`** 是你实际想要的设计颜色。为了统一主题色，我建议执行以下变更：

### 1. 更新全局主题配置
- 修改 `src/styles/index.css` 中的 `--primary` 变量，将其值更新为 `#1a472a` 对应的 HSL 值 (约 `141 46.3% 19%`)。
- 这样 `bg-primary`, `text-primary` 等 Tailwind 类就会自动使用这个新颜色。

### 2. 重构硬编码颜色
将分散在代码中的硬编码颜色 `#1a472a` 替换为 Tailwind 的语义化类名。

-   **`src/pages/HomePage.tsx`**:
    -   `text-[#1a472a]` -> `text-primary`
    -   `bg-[#1a472a]` -> `bg-primary`
    -   `to-[#1a472a]` -> `to-primary`
-   **`src/layouts/MainLayout.tsx`**:
    -   `text-[#1a472a]` -> `text-primary`
    -   `hover:text-[#1a472a]` -> `hover:text-primary`
    -   `bg-[#1a472a]/10` -> `bg-primary/10`
-   **`src/components/3d/ApparelModels.tsx`**:
    -   将常量 `"#1a472a"` 提取为从样式中获取，或统一管理。
-   **`src/components/VerticalTabsSidebar.tsx`**:
    -   `ring-[#1a472a]/20` -> `ring-primary/20`
    -   `shadow-[...rgba(26,71,42...)]` -> 使用 `shadow-primary/xx` 或自定义 shadow。

### 3. 检查关联颜色
- 检查 `HomePage.tsx` 中的 `#0d2e1f` (深色渐变起色)，确认是否需要作为 `primary-foreground` 或 `primary-dark` 添加到配置中。

**你是否同意将主题色统一为 `#1a472a` 并清理这些硬编码？**